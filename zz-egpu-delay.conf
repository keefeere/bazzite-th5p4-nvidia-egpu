# Keep NVIDIA out of the initramfs so that the driver cannot probe the eGPU
# before the USB4/Thunderbolt stack is initialized. The modules remain
# installed in the real root and can be loaded explicitly after boot.
omit_drivers+=" nvidia nvidia_drm nvidia_modeset nvidia_peermem nvidia_uvm "
