# Local override for Bazzite's vendor 99-nvidia.conf.
#
# Keep the internal AMD GPU available in the initramfs, but do not force-load
# NVIDIA before the USB4/Thunderbolt stack has finished initializing.
force_drivers+=" i915 amdgpu "
