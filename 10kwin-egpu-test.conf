# Temporary validation: make the NVIDIA eGPU KWin's primary render device.
# The first DRM device is primary; AMD remains available for the internal panel.
KWIN_DRM_DEVICES=/dev/dri/card0:/dev/dri/card1
